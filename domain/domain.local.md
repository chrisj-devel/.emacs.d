# Domain docs: repo-specific values

This file fills the slots declared in `domain.md`, which owns the rules.
It supplies values; it never overrides one.

`domain.md` declares no slots yet, so this file is empty of them. Add a
`## Slot: <name>` section here only once `domain.md` declares that slot.
